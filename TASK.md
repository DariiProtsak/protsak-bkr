# TASK: CSI Analyzer — десктопний застосунок

## Контекст

Дипломна робота: «Програмно-апаратна система виявлення та аналізу радіоперешкод у бездротовій комп'ютерній мережі», НУ "Львівська політехніка", 2026.

ESP32-S3 (RX) зчитує CSI дані у promiscuous mode і надсилає їх по UART у форматі JSONL на ПК. Ця програма збирає ці дані, навчає ML-модель і класифікує радіоперешкоди. Збирається в `.exe` через PyInstaller.

---

## Числові параметри (обов'язково досягти і показати в UI)

| Параметр | Ціль |
|---|---|
| SVM Accuracy | ≥ 76,9% |
| SVM Macro F1-score | ≥ 0,75 |
| Пакетів на клас для навчання | ≥ 1500 на клас |
| Субнесучих CSI | 114 |
| Пояснена дисперсія PCA-10 | ≥ 93,2% |
| Кількість класів | 3 |

---

## Структура файлів

```
pc_analyzer/
├── main.py
├── config.json                  # зберігається автоматично
├── model.pkl                    # зберігається після тренування
├── pca.pkl                      # зберігається після тренування
├── scaler.pkl                   # зберігається після тренування
├── data/
│   └── dataset.jsonl
├── gui/
│   ├── app.py                   # головне вікно, навігація між екранами
│   ├── screen_init.py           # Екран 1: вибір COM-порту
│   ├── screen_wifi.py           # Екран 2: підключення ESP32 до Wi-Fi
│   ├── screen_menu.py           # Екран 3: головне меню
│   ├── screen_collect.py        # Екран 4: збір навчальних даних
│   └── screen_monitor.py        # Екран 5: моніторинг
└── core/
    ├── serial_reader.py          # читання JSONL з UART
    ├── preprocessor.py           # амплітуди 114 субнесучих, нормалізація
    ├── feature_extractor.py      # PCA-10
    ├── classifier.py             # kNN (k=5) + SVM (RBF)
    └── wifi_commander.py         # команди по UART на ESP32
```

---

## Екран 1 — Вибір COM-порту

- При запуску автоматично перевірити COM3
- Якщо недоступний — показати dropdown зі списком доступних портів
- Кнопка "Підключитись" → відкрити порт (baudrate 115200)
- При успіху → перехід на Екран 2
- Зберегти вибраний порт у `config.json`

---

## Екран 2 — Підключення ESP32 до Wi-Fi

- Поля: SSID, Пароль (з кнопкою показати/приховати)
- Підвантажити збережені значення з `config.json`
- Кнопка "Підключити ESP32" → надіслати по UART:
  ```
  WIFI_CONNECT:<SSID>:<PASSWORD>\n
  ```
- Очікувати відповідь від ESP32: `WIFI_OK` або `WIFI_FAIL` (таймаут 10 сек)
- При успіху → зберегти SSID у `config.json`, перехід на Екран 3

---

## Екран 3 — Головне меню

- Кнопка **[Навчання моделі]** → Екран 4
- Кнопка **[Моніторинг]** → Екран 5 (активна тільки якщо існує `model.pkl`)
- Статус-рядок: поточний COM-порт, статус ESP32

---

## Екран 4 — Навчання моделі

### Крок 1: Збір даних

- Вибір класу: `Порожньо` / `Стілець` / `Людина`
- Поле: тривалість збору (секунди, за замовчуванням 60)
- Кнопка "Старт збору" → читати пакети з UART протягом N секунд
- Прогрес-бар + лічильник пакетів у реальному часі
- Дані зберігаються у `data/dataset.jsonl` з полем `"label"`
- Індикатор: які класи вже зібрано і скільки пакетів

### Крок 2: Тренування

- Кнопка "Тренувати модель" (активна коли зібрано всі 3 класи)
- Pipeline:
  1. Зчитати `dataset.jsonl`
  2. Витягнути амплітуди: `A_k = sqrt(real_k² + imag_k²)` для 114 субнесучих
  3. Нормалізація: `StandardScaler`
  4. PCA → 10 компонент
  5. Розбиття train/test = 80/20 (стратифіковане)
  6. Навчити `kNN (k=5)` та `SVM (kernel='rbf', C=1.0, gamma='scale')`
  7. Зберегти `model.pkl`, `pca.pkl`, `scaler.pkl`

### Виведення результатів

- Таблиця метрик: Accuracy, Precision, Recall, F1 — для kNN і SVM
- F1-score по кожному класу окремо
- Confusion matrix (matplotlib, вбудований у вікно)
- Пояснена дисперсія PCA-10 у відсотках
- Цільові показники підсвічуються зеленим якщо досягнуто:
  - SVM Accuracy ≥ 76.9%
  - SVM F1-macro ≥ 0.75

---

## Екран 5 — Безперервний моніторинг

- Поле: розмір блоку (секунди, за замовчуванням 30)
- Кнопка "Старт" → цикл:
  1. Зібрати пакети протягом N секунд
  2. Обробити: амплітуди → scaler → PCA → класифікація
  3. Показати результат: клас + ймовірність (SVM probability)
  4. Оновити графіки
  5. Повторити
- Кнопка "Стоп" → завершити, показати статистику сесії (% кожного класу)

### Графіки (matplotlib, FigureCanvasTkAgg):

- **Верхній:** амплітуди 114 субнесучих поточного блоку
- **Нижній:** часова шкала класів (кольорові смуги):
  - зелений = Порожньо
  - жовтий = Стілець
  - червоний = Людина

---

## Формат JSONL-пакету від ESP32

```json
{"timestamp": 1234567890, "rssi": -42, "csi": [[r0,i0],[r1,i1],...,[r113,i113]]}
```

Обробка в `preprocessor.py`:
```python
amplitudes = [sqrt(r**2 + i**2) for r, i in packet["csi"]]  # список з 114 float
```

---

## Формат config.json

```json
{
  "com_port": "COM3",
  "ssid": "MyNetwork",
  "collect_duration_sec": 60,
  "monitor_block_sec": 30
}
```

---

## Технічний стек

| Компонент | Бібліотека |
|---|---|
| GUI | `customtkinter` |
| Serial | `pyserial` |
| ML | `scikit-learn` (KNeighborsClassifier, SVC, PCA, StandardScaler) |
| Графіки | `matplotlib` (FigureCanvasTkAgg) |
| Серіалізація | `joblib` |
| Збірка в exe | `PyInstaller` |

---

## Що НЕ входить в scope

- Реалтайм класифікація пакет-за-пакетом (тільки блоками)
- Хмарна синхронізація
- GUI-редактор датасету
- Автоматичне перенавчання
